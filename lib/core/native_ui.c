#include <core/native_ui.h>
#include <core/utils.h>

void IMPL_AssertGUI(char *assertString, usz line, char *file, char *func,
                    char *msg, char *detailedErrorFmt, ...)
{
    UNUSED(assertString);
    UNUSED(line);
    UNUSED(file);
    UNUSED(func);
    UNUSED(msg);
    UNUSED(detailedErrorFmt);
    IMPL();
}

